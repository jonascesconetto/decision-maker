// USE FOR ON PAGE EVENTS ONLY
$(() => {
  $( "#sortable" ).sortable();
  $( "#sortable" ).disableSelection();
  // $.ajax({
  //   method: 'GET',
  //   url: '/api/users'
  // }).done((users) => {
  //   for(user of users) {
  //     $('<div>').text(user.name).appendTo($('body'));
  //   }
  // });
});

